server:
  address: ${PROFILE_SERVICE_HOST:127.0.0.1}
  port: ${PROFILE_SERVICE_PORT:8793}

spring:
  application:
    name: beiming-profile-service
  datasource:
    url: ${PROFILE_DB_URL:jdbc:postgresql://192.168.1.5:5432/beiming?sslmode=disable}
    username: ${PROFILE_DB_USERNAME:beiming}
    password: ${PROFILE_DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  flyway:
    baseline-on-migrate: true
    baseline-version: 0

beiming:
  frontend-origin: ${FRONTEND_ORIGIN:http://127.0.0.1:5173}
  services:
    auth-url: ${AUTH_SERVICE_URL:http://127.0.0.1:8792}
